# CSV Automation

## What it does
Merges multiple CSV files into one, cleans headers, removes duplicates.

## How to use
1. Put CSV files inside `csv_folder`.
2. Run the script:
```bash
python csv_automation.py
